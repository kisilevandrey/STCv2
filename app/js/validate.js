export class ValidateAll {
    constructor(config) {
        this.config = config;
    }
    minLength(id){
        if (minlength){
            return true }
        else {return false}
    };
    maxLength(id){
        if (maxlength){
            return true
        }
        else {return false}
    };



}